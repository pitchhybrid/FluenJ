# Configuração do analyzer (estática de Dart/Flutter) — perfil RIGOROSO.
# Rode `flutter analyze` antes de considerar trabalho concluído.

include: package:flutter_lints/flutter.yaml

analyzer:
  # Rigor de tipos: força casts explícitos, inferência e tipos crus (raw) a
  # serem declarados — pega muitos bugs em tempo de compilação.
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  exclude:
    - 'build/**'
    - '.dart_tool/**'

  # Promove lints críticos a erro (falham o analyze).
  errors:
    avoid_print: error
    avoid_dynamic_calls: error
    always_declare_return_types: error
    unawaited_futures: error
    missing_required_param: error
    missing_return: error
    todo: ignore # TODOs aceitos como info

linter:
  rules:
    # Boas práticas / prevenção de bugs
    avoid_print: true
    avoid_dynamic_calls: true
    always_declare_return_types: true
    unawaited_futures: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    prefer_typing_uninitialized_variables: true

    # const e final (performance e clareza)
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    unnecessary_const: true
    unnecessary_new: true

    # Estilo / clareza
    require_trailing_commas: true
    use_super_parameters: true
    use_key_in_widget_constructors: true
    prefer_single_quotes: true

    # Flutter específico
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true
    use_full_hex_values_for_flutter_colors: true
